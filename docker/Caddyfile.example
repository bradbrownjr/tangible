# Caddy reference snippet for Tangible behind a reverse proxy.
#
# Place this in your Caddyfile (or include it via an `import`).
# Pair with TANGIBLE_BEHIND_PROXY=true and TANGIBLE_ALLOWED_HOSTS=<domain>.

tangible.example.com {
    encode zstd gzip
    reverse_proxy tangible:8000
}
